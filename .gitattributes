# Normalize line endings to LF in the repo so systemd units, the Dockerfile,
# and shell content stay valid on Linux regardless of who commits.
* text=auto eol=lf

*.jpg binary
*.jpeg binary
*.png binary
*.gif binary
*.mp4 binary
